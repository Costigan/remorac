-- Tiny Dense Core convolution-like dot product over one flattened window.
-- Full CNN support still needs window/slice generation and library kernels.
let image_window = [1.0, 2.0, 3.0, 4.0] in
let kernel = [0.25, 0.25, 0.25, 0.25] in
fold (+) 0.0 (map (*) image_window kernel)
