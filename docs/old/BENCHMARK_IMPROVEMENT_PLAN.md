# Benchmark Improvement and Performance Plan

This document is now historical.

The still-relevant performance and benchmarking ideas have been moved to
`docs/ROADMAP.md`, mainly under:

1. `Benchmark Strategy And Performance Targets`;
1. `Representative Pipeline Benchmarks`;
1. `Device-Resident Execution And Plan Composition`;
1. `Kernel Fusion`;
1. `GPU Plan Profiling And IR Introspection`;
1. `CPU Vectorization And Threaded Reductions`;
1. `Benchmarks Against NumPy, JAX, And Futhark`.

The original phase checklist mixed completed implementation notes, benchmark
results from a specific machine, and forward-looking work. Keep current roadmap
items in `docs/ROADMAP.md`; use versioned benchmark result files for measured
numbers.
